# TODO

Свободный реестр того, что хочется сделать. Не план релиза — заметки и
идеи, чтобы было с чего начинать в следующих чатах.

> Сделанное переносим в `CHANGELOG.md`. Здесь — только **открытые**
> задачи и идеи. Крупные реализованные вехи (единый слой маршрутизации,
> sing-box/mihomo, AmneziaWG, прозрачное проксирование, пул серверов,
> курируемые списки, тестер прокси) описаны в README/CHANGELOG.

## Полевое тестирование (приоритет)

Код написан, но не проверялся на реальном железе — нужны прогоны на
устройствах и репорты с `uname -a`:

- [ ] **Firewall TProxy / Redirect / Hybrid + DSCP** на Keenetic
      (KeenOS 4.x/5.x, iptables) и OpenWrt 22.03+ (nftables).
- [ ] **nftset-backend** IP-списков и **/tmp→флешка** на OpenWrt.
- [ ] **AmneziaWG-бинарники** под `mips-softfloat` на старых ядрах
      (musl syscall ABI).
- [ ] **Прозрачный режим + пул серверов** (urltest) под реальной
      нагрузкой LAN.

## AmneziaWG / Configs

- [ ] **QR-код** для конфигов на странице Configs (генерация без
      зависимостей — рисовать PNG/SVG руками либо через встроенный awg).
- [ ] **Импорт `.conf` через QR с камеры** в браузере
      (`navigator.mediaDevices` + jsQR через CDN, опционально).

## Пул серверов / подписки (развитие)

- [ ] **Периодический health-re-test пула** — раз в N часов
      перетестировать `server-pool` и автоматически выкидывать стабильно
      мёртвые серверы из urltest-группы (сейчас фильтр живых только при
      сборке). Опереться на `core/proxy_tester.py`.
- [ ] **Diff при обновлении курируемого списка** — показывать в UI, что
      добавилось/удалилось при последнем refresh (хранится `_remote`
      снимок — данные уже есть).
- [ ] **geosite/geoip .db автообновление** — скачивать и обновлять по
      таймеру базы geosite/geoip для sing-box/mihomo (как курируемые
      списки), чтобы правила `geosite:` всегда были свежими.
- [ ] **Импорт подписки/сервера через QR** (тот же jsQR-путь, что для
      AWG).

## Диагностика / UX

- [ ] **«Почему домен не открывается» → авто-подсказка метода** —
      связать вывод BlockCheck (`remediation`: zapret/tunnel/dns) с
      кнопкой «создать маршрут» нужного типа прямо из результатов
      (вход уже есть: `core/testers` + единый слой).
- [ ] **«Курируемый список → nfqws2-hostlist» напрямую** — кнопка на
      странице «Списки», помимо «→ Маршрут» (для тех, кто не пользуется
      единым слоем).
- [ ] **Профили «режимов»** на главной — один клик переключает набор
      активных туннелей и routing-правил («Дом» / «В дороге» /
      «Стриминг»). Реализовать как пресеты поверх единого слоя.
- [ ] **Уведомления** (Telegram/webhook) при падении туннеля, смене
      сервера в urltest, провале автообновления подписки/списка.

## Наблюдаемость

- [ ] **Метрики Prometheus** на `/metrics` — AWG handshake age,
      нормированный RX/TX, число активных routing-правил, статус пула.
      Полезно тем, у кого Grafana.

## Технический долг

- [ ] **i18n** — UI русскоязычный. Выделить строки в словарь
      (`web/js/i18n/{ru,en}.js`) и переключатель языка.
- [ ] **Расширить покрытие тестами** — осталось без юнит-тестов:
      `strategy_scanner`, `blockcheck`, `diagnostics` (сетевые части),
      `warp_generator`.
- [~] **Слияние** старых страниц «Стратегии» / «Routing (AWG)» в единую
      «Маршрутизацию» — сделан безопасный шаг (кросс-ссылки, «расширенный
      режим»); полное удаление — после полевых тестов.
